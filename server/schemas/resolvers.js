const { User, Theme, Collection, Wishlist, Sets, Post } = require('../models');
// const {
// 	signToken,
// 	AuthenticationError,
// } = require('../utils/auth');

const resolvers = {

}

module.exports = resolvers;